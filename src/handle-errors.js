import ErrorHandler from "./error-handler/component";
import React from "react";
import ReactDOM from "react-dom";

const root = document.querySelector(`.errors`);

ReactDOM.render(<ErrorHandler/>, root);